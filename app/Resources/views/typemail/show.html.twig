{% extends 'base.html.twig' %}

{% block body %}
    <h1>TypeMail</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeMail.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ typeMail.type }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('typemail_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('typemail_edit', { 'id': typeMail.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
